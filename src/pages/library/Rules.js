import TemplatePage from '../../components/TemplatePage';

const Rules = () => (
  <TemplatePage title="📜 Library Rules">
    <p>Display rules for borrowing books, overdue penalties, etc.</p>
  </TemplatePage>
);
export default Rules;