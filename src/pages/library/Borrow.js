import TemplatePage from '../../components/TemplatePage';

const Borrow = () => (
  <TemplatePage title="📦 Borrow & Return">
    <p>Admins or students can request or return books here.</p>
  </TemplatePage>
);
export default Borrow;